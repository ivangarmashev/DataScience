import cv2
import numpy as np

image = cv2.imread("/home/vladislav/1cloud/notebooks/Homework_1_SobelX/test.jpg")
image = cv2.cvtColor(image, cv2.COLOR_BGR2GRAY)

sobel_x = np.array([[-1,0,1], [-2,0,2], [-1,0,1]])
imgx = cv2.filter2D(image, -1, sobel_x)
cv2.imwrite('result.jpg', imgx)
